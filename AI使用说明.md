# AI 使用说明

> 本文记录我在用 AI 开发「可靠事件通知投递服务」过程中的真实协作情况：AI 帮了我什么、我拒绝了它什么、哪些关键决定是我自己拿的。仅作交接与复盘参考，内容以对话实际发生为准。

## 一、AI 在哪些关键地方提供了帮助

1. **把模糊需求梳理成清晰问题**：最初需求只是"业务系统要调外部 API 做通知，要可靠"。是 AI 帮我把问题定位成经典的"可靠投递 / Outbox"问题，并拆出核心维度——至少一次、幂等、重试、供应商差异、投递与业务的解耦。
2. **提供架构选项与取舍分析**：AI 给出了 H2 / MySQL、outbox+轮询 / 消息队列、Kafka / RabbitMQ 等几组对比，把每种的收益和代价摆清楚。这让我能基于自己的流量特征做判断，而不是在空想里选型。
3. **方案细化与完整落地**：AI 把确认后的设计细化为包结构、类职责、表结构、接口契约和测试清单，并完整实现了全部代码与 46 个测试（全部通过）。
4. **技术细节验证与排坑**：AI 用本地 Maven 缓存核对过 Spring Boot 3.5 / Java 21 的具体行为——H2 对 `SKIP LOCKED` 的支持、`JdkClientHttpRequestFactory` 的超时语义（响应到达超时而非 body 读取超时）、`@ConfigurationProperties` 的 record 绑定路径等；并在测试阶段定位修复了若干实现问题（`Clock` bean 缺失、配置前缀绑定路径错误、模板占位符引号、MockWebServer 跨测试串扰）。
5. **文档沉淀**：帮我写 README（含架构、决策、配置、运行）和第一人称设计说明，把讨论中沉淀的判断落成可交接的文字。

## 二、AI 给出过哪些我没有采纳的建议

1. **引入 Kafka（整体架构）**：AI 最初推荐 "MySQL Outbox + Kafka + 消费 Worker" 的生产级架构。我承认它在大流量下是对的，但拒绝了——MVP 阶段它是纯运维负担，outbox + 轮询已经能满足"至少一次"与"削峰"。我改用了 **Outbox + 轮询 + SYNC/ASYNC 双模式**（这个方案是我自己提出的，见下）。
2. **消息队列本身**：AI 在选型里也给了直接生产到 Kafka、RabbitMQ 等选项，我最终**没有采纳任何消息队列**。
3. **告警的完整方案**：AI 建议同时支持 IM Webhook + 邮件，并实现三条告警规则（死信数量阈值、滞留超时阈值、连续失败）。我只采纳了「邮件 + 连续失败」，其余全部列入 README 的 TODO。

## 三、哪些关键决策是我自己做出的，以及原因

1. **MVP 不引入 Kafka，用 Outbox + 轮询 + SYNC/ASYNC 双模式** —— 这是我自己提出的方案。原因：MVP 要快速落地、零新增中间件；而且不同业务的实时性诉求本就不同——广告引流可以接受延迟、订阅付款/购买必须尽快送达。用"同步为主、异步兜底"比引入中间件更贴合实际，也更便宜。
2. **按业务流量特征区分投递模式** —— 我分析了三个场景的流量差异（广告高量突发、订阅必须送达、购买两者兼具），由此得出系统必须同时满足「抗突发」与「不丢消息」两个相反诉求，这直接决定了双模式设计。
3. **按消息指定最大投递次数（maxAttempts）** —— 我提出让业务方按消息重要程度自定重试预算：请求参数优先，缺省取供应商配置，入站快照。
4. **告警的需求与范围** —— "外部系统长期不可用要告警"这个需求是我提出的；范围也是我收窄的：MVP 只做「连续失败 + 邮件」，其余渠道与规则写 TODO，不为还没出现的场景过度建设。
5. **"贴近生产"的基调** —— 我要求即便 MVP 也按生产思路设计（用真实 MySQL、用真实的可靠性语义），但严格控制新增基础设施。
6. **讨论顺序与边界** —— 我决定先讨论存储/中间件、供应商接入方式后议；并在收尾时决定用第一人称写设计说明、新增这份 AI 使用说明。
